import React from "react";

export declare namespace IProductDetailDescriptionBox {
  export interface RefObject {
    
  }

  export interface Props {
    __description?: string;
    isClosed?: boolean;
    children?: React.ReactNode;
  }
}