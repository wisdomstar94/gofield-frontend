import React from "react";

export declare namespace ITitleAndContentRowItem {
  export interface Props {
    __title: React.ReactNode;
    __content: React.ReactNode;

    children?: React.ReactNode;
  }
}