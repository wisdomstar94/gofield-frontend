import React from "react";

export declare namespace IFormbox {
  export interface Props {
    children: React.ReactNode;
  }

  export interface FormboxItemProps {
    __titleComponent: React.ReactNode;
    __contentComponent: React.ReactNode;
  }
}