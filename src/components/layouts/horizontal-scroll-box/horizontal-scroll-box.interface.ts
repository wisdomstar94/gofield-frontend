import React, { CSSProperties } from "react";

export declare namespace IHorizontalScrollBox {
  export interface Props {
    __style?: CSSProperties;
    __scrollLeft?: number;
    children?: React.ReactNode;
  }
}