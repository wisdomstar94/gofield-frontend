import React from "react";

export declare namespace ISwitchToggleButton {
  export interface Props {
    __isActive?: boolean;
    __onChange?: (v: boolean) => void;
    children?: React.ReactNode;
  }
}