export declare namespace ICode {
  export interface CategoryCodeItem {
    id: number;
    name: string;
    thumbnail: string;
  }

  export interface CodeItem {
    name: string;
    code: string;
    group: string;
  }
}